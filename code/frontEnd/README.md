# Frontend

Vue 3 + Vite shell for the local AI video workflow UI.

Suggested next folders:

- `src/views` for workflow pages
- `src/components` for reusable panels
- `src/services` for backend API clients
- `src/stores` for local UI state
